# The basic Python data structures include list, set, tuples, and dictionary.
# [] braces indicates List
# {} braces indicates dictionaries and set (like hashmap(k,v))
# () braces indicates tuple :- nothing but a list but immutable (which cant be changed once declared)

list_sample = ["1", "2", "3"]
dictionary_sample = {1: "1", 2: "2", 3: "3"}
tuple_sample = ("1", "2", "3")
